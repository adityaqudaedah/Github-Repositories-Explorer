import React from "react";

const SearchButton = () => {
  return (
    <button className="text-slate-500 bg-blue-400 hover:bg-blue-500 rounded p-2">
      <span className="text-slate-100 uppercase">search</span>
    </button>
  );
};

export default SearchButton;
